name: littlesteps
description: AI Guardian -> LittleSteps
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.0
  firebase_auth: ^4.6.0
  cloud_firestore: ^4.8.0
  google_sign_in: ^6.1.0
  http: ^0.14.0
  provider: ^6.0.5
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
